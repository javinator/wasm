class Squareroot extends Node {
    	constructor(math) {
        	super();
            	this.name = "Squareroot";
            	this.children = [math];
		}
    	accept() {
    		return visit.visitSquareroot(this);
    	}
}